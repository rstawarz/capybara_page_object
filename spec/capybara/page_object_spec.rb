require 'spec_helper'

describe Capybara::PageObject do
  it 'has a version number' do
    expect(Capybara::PageObject::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
